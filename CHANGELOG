## v0.2.1:

* Convert spaces in queries to %20

## v0.2.0:

* Added support for loading custom config
* Return result body, code & error as string
* Create config object within search rather than cli

## v0.1.0:

* Initial release with search command
